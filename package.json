{
  "name": "gstore",
  "version": "1.0.0",
  "description": "GStore, tienda sencilla con backend propio, admin, PayPal, Cloudinary, Resend y pedidos por WhatsApp.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js && node --check api/index.js && node --check public/js/admin.js && node --check public/js/store.js && node --check public/js/success.js"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "multer": "^2.1.1"
  },
  "engines": {
    "node": "24.x"
  }
}
